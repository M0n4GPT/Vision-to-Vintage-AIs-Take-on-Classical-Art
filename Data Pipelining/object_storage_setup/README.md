# Object Storage Setup

This folder contains instructions and configuration for setting up object storage on CHI@TACC. The object store will be used to persist the training/validation/evaluation dataset (artwork_dataset.zip) for use across multiple compute instances.
